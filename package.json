{
  "name": "probeloop-webmcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ProbeLoop: a human-agent repair bench built with WebMCP.",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
